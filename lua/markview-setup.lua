require("markview").setup({
  tables = {
    enable = false,
    use_virt_lines = false,

    text = {},
    hl = {}
  },
  links = {
    enable = false,

    hyperlinks = {},
    images = {},
    emails = {}
  },
});
